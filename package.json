{
  "name": "chat-app-vercel",
  "version": "1.0.0",
  "description": "Chat application for Vercel deployment",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd react-chat-app && npm run build",
    "start": "vercel dev"
  },
  "dependencies": {
    "react-router-dom": "^7.11.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
