[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
# Use npm install instead of npm ci to avoid lock file issues
cmds = ["npm install --production=false"]

[phases.build]
cmds = []

[start]
cmd = "npx n8n start"

[variables]
# Disable community packages if causing issues
N8N_COMMUNITY_PACKAGES_ENABLED = "false"
